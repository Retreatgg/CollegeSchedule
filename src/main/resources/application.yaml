spring:
  datasource:
    url: jdbc:postgresql://localhost:5050/schedule_db?stringtype=unspecified
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver

  liquibase:
    change-log: classpath:/db/changelog/master.yaml

server:
  address: 172.20.10.3
  port: 9999
